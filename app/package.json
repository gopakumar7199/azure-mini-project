{
  "name": "azure-mini-project",
  "version": "1.0.0",
  "description": "A sample Node.js app for Azure DevOps CI/CD pipeline",
  "scripts": {
    "start": "node ./src/server.js",
    "test": "echo \"No test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
